server:
  port: 8080
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: cc_reggie
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/reggie?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: root
      password: zhaojunren622
  redis:
#    host: 172.24.151.51
    host: 127.0.0.1
    port: 6380
    #    password: 123456
    database: 0
    cache:
      redis:
        time-to-live: 1800000

mybatis-plus:
  configuration:
    #在映射实体或者属性时，将数据库中表名和字段名中的下划线去掉，按照驼峰命名法映射
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: ASSIGN_ID
reggie:
  path: D:\\desktop\\img\\
  ai:
    # 是否启用 AI 大模型（true: 启用，false: 使用本地关键词匹配）
    enabled: true
    # AI 系统提示词
    system-prompt: 你是一个外卖系统的智能客服助手，负责解答用户关于点餐、订单、配送、菜品、优惠活动等问题。请用友好、简洁、专业的语言回答用户问题。如果用户询问菜单推荐，可以推荐热销菜品；如果询问配送时间，告知一般 30-45 分钟；如果询问优惠活动，介绍当前活动。
    # DeepSeek API 配置
    api-key: sk-e40988c271424dd0af781a40a59fffea
    api-url: https://api.deepseek.com/v1/chat/completions
    # 模型名称
    model: deepseek-chat
