# 智能客服模块 - 快速测试指南

## 一、环境准备

### 1.1 数据库配置

打开 MySQL 命令行或数据库管理工具（如 Navicat、DBeaver），执行以下 SQL：

```sql
-- 创建聊天记录表
CREATE TABLE IF NOT EXISTS `chat_record` (
    `id` bigint(20) NOT NULL COMMENT '主键 ID',
    `user_id` bigint(20) DEFAULT NULL COMMENT '用户 ID',
    `question` text COMMENT '用户问题',
    `answer` text COMMENT 'AI 回答',
    `session_id` varchar(100) DEFAULT NULL COMMENT '会话 ID',
    `create_time` datetime DEFAULT NULL COMMENT '创建时间',
    `update_time` datetime DEFAULT NULL COMMENT '更新时间',
    `deleted` int(11) DEFAULT '0' COMMENT '逻辑删除 0-未删除 1-已删除',
    PRIMARY KEY (`id`),
    KEY `idx_session_id` (`session_id`),
    KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='智能客服聊天记录';
```

### 1.2 检查依赖

确认 pom.xml 中已添加以下依赖（应该已经添加）：

```xml
<!-- Spring AI 依赖 -->
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-starter-model-openai</artifactId>
    <version>1.0.0-M4</version>
</dependency>
```

**注意**: 如果 Maven 下载失败，可以暂时注释掉这个依赖，因为当前使用的是模拟 AI 回复，不依赖外部 API。

## 二、启动项目

### 2.1 编译项目

在 IDE 中执行 Maven -> Reload Project 或在命令行执行：

```bash
mvn clean install
```

### 2.2 启动应用

运行 `ReggieApplication.java` 主类，或在命令行执行：

```bash
mvn spring-boot:run
```

看到以下日志表示启动成功：

```
Started ReggieApplication in X.XXX seconds
```

## 三、前端测试

### 3.1 访问系统

打开浏览器访问：http://localhost:8080/front/index.html

### 3.2 登录账号

如果没有账号，需要先注册：
1. 点击登录页面
2. 输入手机号和验证码（如果短信功能未配置，可以使用测试账号）
3. 登录成功后进入首页

### 3.3 进入客服聊天

1. 点击右上角用户头像图标
2. 进入个人中心页面
3. 找到并点击"联系客服"选项
4. 进入聊天界面

### 3.4 测试对话

#### 测试场景 1：配送时间
**输入**: "配送多久能到？"
**预期回复**: "我们的配送时间一般为 30-45 分钟，具体取决于您的距离和订单量。高峰期可能略有延迟，敬请谅解！"

#### 测试场景 2：配送费
**输入**: "配送费怎么算？"
**预期回复**: "配送费为 6 元，满 50 元免配送费哦！"

#### 测试场景 3：优惠活动
**输入**: "有什么优惠活动？"
**预期回复**: "新用户注册即送 18 元红包！每日签到也可领取优惠券。关注我们的公众号还有更多惊喜活动！"

#### 测试场景 4：退款
**输入**: "如何申请退款？"
**预期回复**: "如需退款，请在订单页面申请退款，我们会在 1-3 个工作日内处理。"

#### 测试场景 5：菜品推荐
**输入**: "推荐几个招牌菜"
**预期回复**: "我们的招牌菜有：宫保鸡丁、鱼香肉丝、麻婆豆腐，都是顾客们的好评菜品哦！"

#### 测试场景 6：营业时间
**输入**: "你们营业到几点？"
**预期回复**: "我们的营业时间为每天 10:00-22:00，欢迎您光临！"

#### 测试场景 7：客服电话
**输入**: "客服电话是多少？"
**预期回复**: "我们的客服电话是 400-123-4567，工作时间为 9:00-21:00。"

#### 测试场景 8：未知问题
**输入**: "今天天气怎么样？"
**预期回复**: 随机返回通用回复之一
- "感谢您的咨询！我已经记录了您的问题，会尽快为您解答。请问还有其他可以帮助您的吗？"
- "您好！关于这个问题，建议您查看我们的帮助中心..."
- "收到您的问题啦！我正在努力学习中..."
- "非常抱歉，我还没有完全理解您的问题..."

### 3.5 快捷问题测试

点击聊天界面底部的快捷问题按钮：
- "配送时间多久？"
- "配送费怎么算？"
- "有什么优惠活动？"
- "如何申请退款？"
- "推荐几个招牌菜"

每个快捷问题都应该得到正确的回复。

### 3.6 历史记录测试

1. 发送几条消息
2. 关闭聊天页面
3. 重新进入客服聊天
4. 应该能看到之前的聊天记录

## 四、后端 API 测试

### 4.1 使用 Postman 测试

#### 测试发送消息接口

**请求**:
- Method: POST
- URL: http://localhost:8080/chat/send
- Headers:
  - Content-Type: application/json
  - userId: 123456789 (任意用户 ID)
- Body (JSON):
```json
{
  "question": "配送费多少钱？",
  "sessionId": "test_session_001"
}
```

**预期响应**:
```json
{
  "code": 1,
  "msg": "success",
  "data": {
    "answer": "配送费为 6 元，满 50 元免配送费哦！",
    "sessionId": "test_session_001"
  }
}
```

#### 测试获取历史记录接口

**请求**:
- Method: GET
- URL: http://localhost:8080/chat/history?sessionId=test_session_001

**预期响应**:
```json
{
  "code": 1,
  "msg": "success",
  "data": [
    {
      "id": "xxx",
      "userId": "123456789",
      "question": "配送费多少钱？",
      "answer": "配送费为 6 元，满 50 元免配送费哦！",
      "sessionId": "test_session_001",
      "createTime": "2026-03-31T10:00:00"
    }
  ]
}
```

## 五、数据库验证

执行以下 SQL 查看聊天记录：

```sql
SELECT * FROM chat_record ORDER BY create_time DESC LIMIT 10;
```

应该能看到刚才测试的聊天记录。

## 六、性能测试（可选）

### 6.1 并发测试

使用 JMeter 或其他工具，模拟多个用户同时发送消息：
- 线程数：10
- Ramp-Up 时间：1 秒
- 循环次数：5

观察系统响应时间和数据库记录。

### 6.2 压力测试

持续发送大量消息，观察：
- 内存使用情况
- CPU 使用率
- 数据库连接数
- 响应时间变化

## 七、常见问题排查

### 问题 1：启动时报依赖错误

**解决方案**: 
- 检查网络连接
- 清理 Maven 本地仓库：`rm -rf ~/.m2/repository/org/springframework/ai`
- 重新下载依赖：`mvn clean install -U`
- 或者暂时注释掉 Spring AI 依赖

### 问题 2：前端页面无法访问

**排查步骤**:
1. 检查项目是否启动成功
2. 确认端口是否为 8080
3. 检查防火墙设置
4. 尝试访问 http://127.0.0.1:8080

### 问题 3：登录后看不到"联系客服"选项

**排查步骤**:
1. 清除浏览器缓存
2. 刷新页面（Ctrl+F5 强制刷新）
3. 检查 user.html 是否正确修改
4. 查看浏览器控制台是否有 JavaScript 错误

### 问题 4：发送消息后没有回复

**排查步骤**:
1. 打开浏览器控制台（F12）
2. 查看 Network 标签，检查请求是否发送成功
3. 查看后端日志，确认是否收到请求
4. 检查数据库中是否保存了记录

### 问题 5：数据库连接失败

**解决方案**:
- 检查 MySQL 服务是否启动
- 确认 application.yml 中的数据库配置正确
- 验证用户名密码是否正确
- 检查数据库 reggie 是否存在

## 八、面试演示建议

### 8.1 演示流程

1. **介绍功能**（1 分钟）
   - 智能客服的作用
   - 主要功能点
   
2. **现场演示**（2 分钟）
   - 展示聊天界面
   - 提问 2-3 个常见问题
   - 展示历史记录功能
   
3. **技术讲解**（2 分钟）
   - 后端架构：Controller -> Service -> Mapper
   - 数据库设计
   - 前端 Vue.js 实现
   
4. **扩展说明**（1 分钟）
   - 如何集成真实 AI
   - 未来优化方向

### 8.2 可能被问到的问题

准备好回答以下问题：
1. 为什么选择关键词匹配而不是 NLP？
2. 如果要集成 GPT，你会怎么做？
3. 聊天记录太多怎么办？
4. 如何实现多轮对话（上下文理解）？
5. 如何评估客服机器人的质量？

## 九、测试检查清单

完成以下所有项的测试：

- [ ] 数据库表创建成功
- [ ] 项目启动成功
- [ ] 能够登录系统
- [ ] 进入个人中心看到"联系客服"选项
- [ ] 点击进入聊天页面
- [ ] 发送消息获得回复
- [ ] 测试至少 5 个不同的问题
- [ ] 使用快捷问题功能
- [ ] 查看历史记录功能正常
- [ ] 数据库中有聊天记录
- [ ] 使用 Postman 测试 API 接口

---

**祝你测试顺利！如有问题，请查看智能客服说明.md 文档。**
